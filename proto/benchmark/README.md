# Benchmarks

The production native benchmark runner is:

```powershell
python python\tests\benchmark_native.py --profile small --destination-root D:\qfc-bench --workers 1,4,8,16
```

This directory holds the data generator, runner, and reports created for the former
Python prototype. They are historical measurements and should not be presented as
results from the current QuickFileCopy native engine.
