# QuickFileCopy 未ビルド成果物の再ビルド v1.0

実施日: 2026-08-22
対象: `C:\Users\makta\source\QuickFileCopy`

## 1. 企画

Quickシリーズのうち `dist/binary` に実行ファイルが無いアプリをビルドし、配布可能な成果物を復元する。

## 2. 計画

1. `build.bat`（内部で `build_native.py`）を実行する。
2. `dist/binary/QuickFileCopy.exe`、`QuickFileCopy_cli.exe`、`WebView2Loader.dll` の生成を確認する。
3. 可能なら CLI ヘルプとネイティブスモークテストを実行する。

## 3. 完了条件

- `dist/binary` に GUI / CLI / WebView2 ローダーが存在する。
- 実施結果を本計画に対応する結果書へ記録する。
